# {{name}}

This is a Solid web app project. Keep operational UI in `src/`, use Trellis Records for structured app data, and use Content/Links projections for supporting material.

Useful commands:

```bash
bun install
bun run dev
bun run build
bun run typecheck
```
