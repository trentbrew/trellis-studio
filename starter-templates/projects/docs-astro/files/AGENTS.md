# {{name}}

This is an Astro docs project. Keep pages in `src/pages/`, longer docs in `src/content/docs/`, and use Trellis article/content projections to plan and manage publishing work.

Useful commands:

```bash
bun install
bun run dev
bun run build
bun run typecheck
```
