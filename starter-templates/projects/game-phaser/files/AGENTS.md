# {{name}}

This is a Phaser game project. Keep game logic in `src/`, model levels and entities in Trellis CMS projections, and track sprites/media in Assets.

Useful commands:

```bash
bun install
bun run dev
bun run build
bun run typecheck
```
