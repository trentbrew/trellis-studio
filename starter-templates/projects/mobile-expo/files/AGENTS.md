# {{name}}

This is an Expo mobile app project. Keep app screens in `App.tsx` or split them into `src/` as the project grows. Use Trellis projections for notes, app records, content, and links.

Useful commands:

```bash
bun install
bun run start
bun run ios
bun run android
bun run typecheck
```
