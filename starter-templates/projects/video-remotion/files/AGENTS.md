# {{name}}

This is a Remotion video project. Keep compositions in `src/`, write structured scene/script data in the Trellis CMS projections, and render final videos into `out/`.

Useful commands:

```bash
bun install
bun run dev
bun run render
bun run typecheck
```
